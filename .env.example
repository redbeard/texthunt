# Copy to `.env` and fill in real values. `.env` is gitignored — never commit it.
# Slack token used to export message history (a user token, scope: channels:history).
SLACK_TOKEN=xoxp-your-token-here
